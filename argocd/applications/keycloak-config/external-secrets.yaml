apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-client-argocd-secret
  annotations:
    argocd.argoproj.io/sync-wave: "100"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: keycloak-client-argocd-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: "kubernetes/test/keycloak/keycloak-client-argocd-secret" # todo: adjust to your secret provider
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-realm-user-max
  annotations:
    argocd.argoproj.io/sync-wave: "100"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: keycloak-realm-user-max
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: "kubernetes/test/keycloak/keycloak-realm-user-max" # todo: adjust to your secret provider
